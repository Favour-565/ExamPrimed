import SignUpForm from "../components/auth/SignUpForm";

function SignupPage() {
    return (
      <main className="fixed inset-0 w-full h-full overflow-y-auto bg-white">
        <SignUpForm />
      </main>
    );
  }

export default SignupPage;